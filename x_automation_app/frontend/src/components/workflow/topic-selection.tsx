import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function TopicSelection() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Action: Select a Topic</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Topic selection UI will be rendered here.</p>
        {/* Full implementation in Step 4.4.1 */}
      </CardContent>
    </Card>
  )
} 